# XXE外部实体注入 — 漏洞防护

---

## 一、禁用外部实体

这是最根本的防护措施：

```java
// ✅ 安全配置：禁用外部实体
DocumentBuilderFactory factory = DocumentBuilderFactory.newInstance();
factory.setFeature("http://apache.org/xml/features/disallow-doctype-decl", true);
factory.setFeature("http://xml.org/sax/features/external-general-entities", false);
factory.setFeature("http://xml.org/sax/features/external-parameter-entities", false);
factory.setXIncludeAware(false);
factory.setExpandEntityReferences(false);
```

---

## 二、使用安全解析器

### 1. 使用SAXParser安全配置

```java
SAXParserFactory factory = SAXParserFactory.newInstance();
factory.setFeature("http://apache.org/xml/features/disallow-doctype-decl", true);
factory.setFeature("http://xml.org/sax/features/external-general-entities", false);
```

### 2. 不接受XML输入

如果业务不需要XML，移除对 `application/xml` 的支持：

```java
// ✅ 只接受JSON
@PostMapping(value = "/update", consumes = "application/json")
```

---

## 防护总结

| 漏洞类型 | 核心防护 | 关键代码改动 |
|---------|---------|-------------|
| 有回显XXE | 禁用外部实体 | `setFeature("disallow-doctype-decl", true)` |
| Blind XXE | 禁用外部实体+禁止网络请求 | 同上+防火墙限制出站 |
| Content-Type切换XXE | 移除XML支持 | `consumes = "application/json"` |

**核心原则**：如果必须解析XML，务必禁用外部实体和DOCTYPE声明；如果不需要XML，不要接受XML输入。
